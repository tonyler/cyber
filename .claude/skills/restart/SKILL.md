# Restart All Services

Stop and restart all services using the scripts in `/scripts`.

## Steps

1. Stop all services:
   ```bash
   ./scripts/stop_all.sh
   ```

2. Start all services:
   ```bash
   ./scripts/start_all.sh
   ```

3. Confirm services are running
